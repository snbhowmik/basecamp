# Basecamp v1.0.0 — Environment Configuration
# Copy to .env, fill in real values, and NEVER commit .env to git.
# All of these belong in Docker secrets in production, not a plain env file
# sitting on the VPS — this .env is for local development only.

# --- Postgres ---
POSTGRES_PASSWORD=

# --- Auth / JWT ---
# Generate with: openssl rand -base64 48
JWT_SECRET=
# The anon and service_role keys are JWTs signed with JWT_SECRET, containing
# {"role": "anon"} and {"role": "service_role"} respectively. Generate these
# with the Supabase CLI or a JWT library once JWT_SECRET is set.
ANON_KEY=
SERVICE_ROLE_KEY=

# --- Site ---
SITE_URL=https://portal.example.edu

# --- SMTP (required — GOTRUE_MAILER_AUTOCONFIRM is "false", so nobody can
# confirm a signup, including the wizard's own captain account, without a
# working SMTP relay). Any real SMTP server works, e.g. self-hosted Mailcow
# on your own domain — get SPF/DKIM/DMARC right on that domain first.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_ADMIN_EMAIL=
SMTP_SENDER_NAME=Basecamp

# --- Mailer worker ---
# Password for the basecamp_mailer Postgres role, which the `mailer` service
# connects as to send invite emails. Generate with: openssl rand -hex 24
# Deliberately NOT reusing POSTGRES_PASSWORD — this role is the least
# privileged thing in the stack and shouldn't share a credential with the
# superuser. The role itself is created by 0008_invite_email.sql; the
# password is set by scripts/bootstrap-db-roles.sh, which must therefore be
# re-run after migrations are applied.
MAILER_DB_PASSWORD=

# --- Garage (self-hosted object storage) ---
GARAGE_RPC_SECRET=
GARAGE_ACCESS_KEY=
GARAGE_SECRET_KEY=
# Mount point of a separately attached, independently growable block volume
# — never the boot disk. Provider-specific (e.g. DigitalOcean:
# /mnt/<volume-name>). For local dev only, a plain host directory is fine:
# GARAGE_DATA_HOST_PATH=./.data/garage
GARAGE_DATA_HOST_PATH=

# --- Realtime (Elixir/Phoenix) ---
# Generate with: openssl rand -base64 64
REALTIME_SECRET_KEY_BASE=
# Generate with: openssl rand -hex 16
REALTIME_DB_ENC_KEY=

# --- Offsite encrypted backup target (R2 or B2) ---
# This is a BACKUP destination only — never live storage. See NOTE.md.
BACKUP_S3_ENDPOINT=
BACKUP_S3_ACCESS_KEY=
BACKUP_S3_SECRET_KEY=
BACKUP_S3_BUCKET=
BACKUP_ENCRYPTION_KEY=
