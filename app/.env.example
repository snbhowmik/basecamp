# Copy to .env.local — never commit real values.
# These are the PUBLIC anon key + URL only. The service_role key must never
# appear in frontend code or env files — see root README.md / NOTE.md.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
